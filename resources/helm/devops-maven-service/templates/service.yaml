apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-maven-service.fullname" . }}
  labels:
    {{- include "devops-maven-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.services }}
    - port: {{ .port }}
      targetPort: {{ .name }}
      protocol: {{ .protocol }}
      name: {{ .name }}
    {{- end }}
  selector:
    {{- include "devops-maven-service.selectorLabels" . | nindent 4 }}
